const TipsCopy = [
    "Do not add traits to the objects not specifically mentioned in the description.",
    "Do not add items to the interaction other than the two specifically selected.",
    "Make sure the interaction is BETWEEN the two objects."
]

export default TipsCopy;
